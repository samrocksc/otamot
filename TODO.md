## TODO

## Fixed

- For some reason the todo markdown is being included inside of the note, it should only be inside of the markdown file. the note is specifically for notes about that period. It should not have anything related to the TODO list or the kanban board
- The collapsable sidebar looks horrible, can you set it up to use the arrow from the demo on the egui website?
- robotic lime kanban draggable area should be _slightly_ lighter than the rest of the board, so the user can see the outline of the draggable object.
- For some reason when the TODO item is deleted, it's not being deleted from the Kanban board, which is my expected behaviour.
- In the robotic lime theme, the save button in settings text is the same colour as the button, this should be black, or something complimentary
- In Monokai light, we still have a background that is black on the main app, it should be white.
- In monokai light, the blue _clear_ button isn't very readable because the text is black, it should be white text on a blue button
- Let's add a slider with 1-30 in the work/break durations instead of using the plus/minus
- In Monokai white, the text boxes in settings are white on white, we should have an border of the text box that is a lightish gray
- The TODO list should be linked with the KANBAN list and considered the same feature. When you add a TODO item, it should be added to the KANBAN board, and when you delete a TODO item, it should be deleted from the KANBAN board. The TODO list is just a way to quickly add items to the KANBAN board, and the KANBAN board is where you can manage those items. They should each read from the same markdown file
- The hashtag selector is a bit buggy. I don't want to save the hashtag until I have hit _enter_ inside of the hashtag selector.
