## 1.0-

## 0.3 - TODO and stability

- We need to make sure that when hitting tab inside of the editor, it doesn't escape the editor box. It should perform the edit function of the editor.
- dThe TODO item checkboxes do not work.
- The TODO should be able to be toggled on and off
- When a todo item is checked, it should move into a _completed_ section of the JSON with a timestamp
- Even when hitting clear completed, it should still save it to historical completed context with its timestamp
- the TODO list is cut off when it's toggled, the window should grow to encompass it

  0.4 - Editor Improvements

- Check if there is an editor package that we could use for this instead of building all markdown settings manually
- When hitting enter on an unordered or ordered list in markdown, I’d like for the editor to automatically insert another precursor(e.g. \*,-,2.). If there is an markdown editor library to do this, please use this.
- If I hit tab on an list item, then it should indent the line two spaces

### .5 - Skinning

- improve and normalize

## 1.0 - First Major

- TODO
- kanban
- Notes
- Timer Settings
- skinning

  1.1 - Notifications
  1.2 - First implementation of ADHD tooling
  1.3 - Sound Improvements
