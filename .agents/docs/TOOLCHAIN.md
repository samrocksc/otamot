# Toolchain

- Rust 1.70+ with `eframe` 0.31 for cross-platform GUI (macOS + Linux)
- `glow` renderer for OpenGL backend (no Metal/Metal-specific dependencies)
- Tests use `tempfile` crate for isolated file I/O testing
